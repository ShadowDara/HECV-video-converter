name: cli
description: A HECV command line Video Converter
version: 0.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  file_picker_desktop: ^1.1.1

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.6
